{
  "type": "screen",
  "source": {
    "layout": "fixtures/Embed/navigationMode__isolated_pop_boundary.layout.json"
  },
  "metadata": {
    "name": "conformance Embed.navigationMode (isolated_pop_boundary)",
    "description": "Embed semantic conformance fixture (isolated_pop_boundary). Companion embedded screens live under fixtures/Embed/__screens/ — see manifest 'companions'.",
    "generatedBy": "@generated jui conformance generate — DO NOT EDIT",
    "tags": [
      "conformance",
      "embed",
      "Embed"
    ]
  },
  "platform": "all",
  "cases": [
    {
      "name": "navigationMode__isolated_pop_boundary",
      "description": "Embed semantic conformance fixture (isolated_pop_boundary). Companion embedded screens live under fixtures/Embed/__screens/ — see manifest 'companions'.",
      "steps": [
        {
          "action": "waitFor",
          "id": "root"
        },
        {
          "action": "tap",
          "id": "push-button"
        },
        {
          "action": "waitFor",
          "id": "embed-second-label"
        },
        {
          "assert": "text",
          "id": "embed-second-label",
          "equals": "embed-second"
        },
        {
          "action": "tap",
          "id": "pop-button"
        },
        {
          "action": "waitFor",
          "id": "embed-root-label"
        },
        {
          "assert": "text",
          "id": "embed-root-label",
          "equals": "embed-root"
        },
        {
          "action": "tap",
          "id": "pop-button"
        },
        {
          "action": "waitFor",
          "id": "embed-root-label"
        },
        {
          "assert": "text",
          "id": "embed-root-label",
          "equals": "embed-root"
        },
        {
          "assert": "text",
          "id": "host-marker",
          "equals": "host"
        }
      ]
    }
  ]
}
