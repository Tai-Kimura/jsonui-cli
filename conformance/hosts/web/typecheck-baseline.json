{
  "_comment": [
    "Known type errors in the generated web host, pinned so new ones fail CI (scripts/typecheck_ratchet.mjs).",
    "The host is built by the production rjui codegen, so an error here is an error in every consumer's",
    "@generated tree — and an @generated file cannot be patched downstream. When the count reaches 0 the",
    "ratchet is replaced by a plain `npm run typecheck` gate and this file is deleted (plan 44 Phase 4).",
    "THE COUNT IS NOW 0. Every remaining error was a fixture layout binding data it never declared; the",
    "interactive fixture generator now declares its handlers and its deliberately-unprovisioned keys",
    "(plan 44 Phase 2, landed by plan 34 immediately before its re-measure). Phase 4 can delete this file.",
    "History: 89 at the start of plan 37 (2026-08-04) → 38 after the TabView/TextView codegen fixes and the",
    "Slider double-style fix from plan 34 → 37 after the Radio value fix (plan 44 Phase 0) → 10 after the",
    "fixtureRegistry typing and the stray @ts-expect-error (plan 44 Phase 1) → 0 after the data-section",
    "declarations, the `Any` TypeScript mapping and the duplicate-identifier guard (plan 44 Phase 2)."
  ],
  "maxErrors": 0,
  "knownFiles": []
}
