"""Typed attribute extraction code generator (Renderer SSoT pillar C).

Generates Swift / Kotlin / Ruby typed parsers from
``shared/core/attribute_definitions.json`` so hand-written converters
receive typed objects instead of raw JSON dictionaries.
"""
