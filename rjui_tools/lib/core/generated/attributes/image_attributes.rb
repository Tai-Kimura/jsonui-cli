# @generated by `jui generate attr-bindings` — DO NOT EDIT BY HAND.
# Source of truth: shared/core/attribute_definitions.json
# Regenerate: jui generate attr-bindings --lang ruby
# frozen_string_literal: true

require_relative 'attr_support'
require_relative 'common_attributes'

module JsonUI
  module Generated
    # Typed attribute extraction for the `Image` component.
    # Overrides the common definition of: `canTap`.
    module ImageAttributes
      # Declared-attribute rows — part of the public metadata
      # contract together with `rows` / `declared?` / `alias_map`
      # (see the directory README).
      ATTRS = [
        # Alt text for accessibility
        { name: 'alt', kind: :string }.freeze,
        # Enable tap gesture independent of onClick.
        { name: 'canTap', kind: :boolean }.freeze,
        # Content mode (binding supported)
        { name: 'contentMode', kind: :enum, bindable: true, values: ['fit', 'fill', 'center', 'top', 'bottom', 'left', 'right', 'AspectFill', 'AspectFit', 'Center', 'ScaleToFill', 'Top', 'Bottom', 'Left', 'Right'].freeze }.freeze,
        # Fallback image on load error.
        { name: 'errorImage', kind: :string }.freeze,
        # Image when highlighted (binding supported)
        { name: 'highlightSrc', kind: :string, bindable: true }.freeze,
        # Highlight image source name (binding supported)
        { name: 'highlightSrcName', kind: :string, bindable: true }.freeze,
        # Loading behavior
        { name: 'loading', kind: :enum, values: ['lazy', 'eager'].freeze }.freeze,
        # Placeholder shown while the main image loads.
        { name: 'loadingImage', kind: :string }.freeze,
        # Pinch-zoom upper bound.
        { name: 'maxZoom', kind: :number }.freeze,
        # Pinch-zoom lower bound.
        { name: 'minZoom', kind: :number }.freeze,
        # Rendering mode
        { name: 'renderingMode', kind: :enum, values: ['original', 'template'].freeze }.freeze,
        # Image source name (can be data binding)
        { name: 'src', kind: :string, bindable: true }.freeze,
        # Image source name (alias, can be data binding)
        { name: 'srcName', kind: :string, bindable: true }.freeze,
        # Interpret `src` as an SF Symbol name.
        { name: 'systemIcon', kind: :boolean }.freeze,
      ].freeze

      # Returns a Hash keyed by canonical attribute name.
      # `canonical_only: true` disables alias fallback for
      # L1-normalized input (aliases are already rewritten).
      # Common attributes are merged first; component-level
      # definitions override on name collision.
      def self.extract(hash, canonical_only: false)
        out = CommonAttributes.extract(hash, canonical_only: canonical_only)
        out.merge!(AttrCoerce.extract_table(hash, ATTRS, 'Image',
                                            canonical_only: canonical_only))
        out
      end

      # Canonical name => extraction row (common rows merged
      # first; component rows override on name collision).
      def self.rows
        @rows ||= AttrTable.build_rows(CommonAttributes::ATTRS, ATTRS)
      end

      # True when `key` is a declared canonical name or alias
      # spelling.
      def self.declared?(key)
        rows.key?(key) || alias_map.key?(key)
      end

      # Alias spelling => canonical attribute name.
      def self.alias_map
        @alias_map ||= AttrTable.build_alias_map(rows)
      end
    end
  end
end
