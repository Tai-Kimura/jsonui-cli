# @generated by `jui generate attr-bindings` — DO NOT EDIT BY HAND.
# Source of truth: shared/core/attribute_definitions.json
# Regenerate: jui generate attr-bindings --lang ruby
# frozen_string_literal: true

require_relative 'attr_support'
require_relative 'common_attributes'

module JsonUI
  module Generated
    # Typed attribute extraction for the `Progress` component.
    # Overrides the common definition of: `tintColor`.
    module ProgressAttributes
      # Declared-attribute rows — part of the public metadata
      # contract together with `rows` / `declared?` / `alias_map`
      # (see the directory README).
      ATTRS = [
        # ActivityIndicator color, distinct from tintColor for historical reasons.
        { name: 'color', kind: :color }.freeze,
        # ActivityIndicator: auto-hide when stopped.
        { name: 'hidesWhenStopped', kind: :boolean }.freeze,
        # ActivityIndicator size style.
        { name: 'indicatorStyle', kind: :enum, values: ['medium', 'large'].freeze }.freeze,
        # Progress value (0-1, can be binding)
        { name: 'progress', kind: :number, bindable: true }.freeze,
        # Custom progress image
        { name: 'progressImage', kind: :string }.freeze,
        # Progress portion color - hex string or color name from colors.json (binding supported)
        { name: 'progressTintColor', kind: :string, bindable: true }.freeze,
        # Progress view style: default, bar
        { name: 'progressViewStyle', kind: :string }.freeze,
        # Tint color - hex string or color name from colors.json
        { name: 'tintColor', kind: :string }.freeze,
        # Custom track image
        { name: 'trackImage', kind: :string }.freeze,
        # Track portion color - hex string or color name from colors.json (binding supported)
        { name: 'trackTintColor', kind: :string, bindable: true }.freeze,
      ].freeze

      # Returns a Hash keyed by canonical attribute name.
      # `canonical_only: true` disables alias fallback for
      # L1-normalized input (aliases are already rewritten).
      # Common attributes are merged first; component-level
      # definitions override on name collision.
      def self.extract(hash, canonical_only: false)
        out = CommonAttributes.extract(hash, canonical_only: canonical_only)
        out.merge!(AttrCoerce.extract_table(hash, ATTRS, 'Progress',
                                            canonical_only: canonical_only))
        out
      end

      # Canonical name => extraction row (common rows merged
      # first; component rows override on name collision).
      def self.rows
        @rows ||= AttrTable.build_rows(CommonAttributes::ATTRS, ATTRS)
      end

      # True when `key` is a declared canonical name or alias
      # spelling.
      def self.declared?(key)
        rows.key?(key) || alias_map.key?(key)
      end

      # Alias spelling => canonical attribute name.
      def self.alias_map
        @alias_map ||= AttrTable.build_alias_map(rows)
      end
    end
  end
end
