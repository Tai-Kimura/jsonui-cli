# @generated by `jui generate attr-bindings` — DO NOT EDIT BY HAND.
# Source of truth: shared/core/attribute_definitions.json
# Regenerate: jui generate attr-bindings --lang ruby
# frozen_string_literal: true

require_relative 'attr_support'
require_relative 'common_attributes'

module JsonUI
  module Generated
    # Typed attribute extraction for the `View` component.
    # Overrides the common definition of: `distribution`.
    module ViewAttributes
      # Declared-attribute rows — part of the public metadata
      # contract together with `rows` / `declared?` / `alias_map`
      # (see the directory README).
      ATTRS = [
        # Child component(s)
        { name: 'child', kind: :array }.freeze,
        # Child components (alias for child)
        { name: 'children', kind: :array }.freeze,
        # Layout direction
        { name: 'direction', kind: :enum, values: ['topToBottom', 'bottomToTop', 'leftToRight', 'rightToLeft', 'none'].freeze }.freeze,
        # Child distribution mode for stacks
        { name: 'distribution', kind: :enum, values: ['fill', 'fillEqually', 'equalSpacing', 'equalCentering'].freeze }.freeze,
        # Make this view draggable
        { name: 'draggable', kind: :boolean }.freeze,
        # Flex wrap behavior
        { name: 'flexWrap', kind: :enum, values: ['nowrap', 'wrap', 'wrap-reverse'].freeze }.freeze,
        # Gradient colors
        { name: 'gradient', kind: :array }.freeze,
        # Gradient direction
        { name: 'gradientDirection', kind: :string }.freeze,
        # View highlighted state
        { name: 'highlighted', kind: :boolean }.freeze,
        # Gradient color stop locations
        { name: 'locations', kind: :array }.freeze,
        # Handler called when a dragged item enters this view - @{handlerName}
        { name: 'onDragEnter', kind: :binding }.freeze,
        # Handler called when a dragged item leaves this view - @{handlerName}
        { name: 'onDragLeave', kind: :binding }.freeze,
        # Handler called while a dragged item hovers over this view - @{handlerName}
        { name: 'onDragOver', kind: :binding }.freeze,
        # Handler called when drag starts - @{handlerName}
        { name: 'onDragStart', kind: :binding }.freeze,
        # Handler called when an item is dropped onto this view - @{handlerName}
        { name: 'onDrop', kind: :binding }.freeze,
        # Stack orientation (default: zstack)
        { name: 'orientation', kind: :enum, values: ['horizontal', 'vertical'].freeze }.freeze,
        # Safe area inset positions (SafeAreaView only)
        { name: 'safeAreaInsetPositions', kind: :array }.freeze,
        # Space between children (binding supported)
        { name: 'spacing', kind: :number, bindable: true }.freeze,
      ].freeze

      # Returns a Hash keyed by canonical attribute name.
      # `canonical_only: true` disables alias fallback for
      # L1-normalized input (aliases are already rewritten).
      # Common attributes are merged first; component-level
      # definitions override on name collision.
      def self.extract(hash, canonical_only: false)
        out = CommonAttributes.extract(hash, canonical_only: canonical_only)
        out.merge!(AttrCoerce.extract_table(hash, ATTRS, 'View',
                                            canonical_only: canonical_only))
        out
      end

      # Canonical name => extraction row (common rows merged
      # first; component rows override on name collision).
      def self.rows
        @rows ||= AttrTable.build_rows(CommonAttributes::ATTRS, ATTRS)
      end

      # True when `key` is a declared canonical name or alias
      # spelling.
      def self.declared?(key)
        rows.key?(key) || alias_map.key?(key)
      end

      # Alias spelling => canonical attribute name.
      def self.alias_map
        @alias_map ||= AttrTable.build_alias_map(rows)
      end
    end
  end
end
